name: Deploy Aeon Quiz Module

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
      
      - name: Deploy to Staging Server
        run: |
          # Setup known_hosts
          ssh-keyscan aeon.lookmedia.co.id >> ~/.ssh/known_hosts
          
          # Sync plugin to server
          rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no" \
            ./ ${{ secrets.SERVER_USER }}@aeon.lookmedia.co.id:/opt/aeon-lms-web/plugins/mod_aeonquiz/
          
          # Restart Moodle container
          ssh ${{ secrets.SERVER_USER }}@aeon.lookmedia.co.id \
            "cd /opt/aeon-lms-web && docker compose restart moodle"
          
          echo "✅ Plugin mod_aeonquiz deployed to aeon.lookmedia.co.id!"
